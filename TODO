
 - LICENSE
 - separate action for first installation and reconfigure (in order to add public key)
 - more sanity check (removing .ssh, be sure another user doesn't exist, advises admin that
   there is an entry for the user in sudo, etc...)
